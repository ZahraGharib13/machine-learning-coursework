# Dataset

The dataset is not included in this repository.

Required local file:

```text
student_version2.csv
```

Place it in this folder:

```text
data/
├── README.md
└── student_version2.csv
```

The notebook uses the relative path:

```python
pd.read_csv("data/student_version2.csv", low_memory=False)
```

A public source for this dataset has not been confirmed, so no download link is provided here.
