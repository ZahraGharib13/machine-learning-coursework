# Generated Outputs

This folder is reserved for files created by the notebook:

- `submission.csv` — predicted fraud labels for the provided test data;
- `fraud_detection_pipeline.joblib` — the complete fitted preprocessing + SVC pipeline;
- `model_artifacts.zip` — packaged generated artifacts.

They are not bundled here because the input datasets are not included.
