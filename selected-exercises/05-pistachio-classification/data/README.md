# Dataset

Required local files:

```text
train.csv
test.csv
```

The labeled training file contains the target column:

```text
Class
```

The data files are intentionally not bundled. Add the original public source link here once the exact source and redistribution terms are confirmed.
