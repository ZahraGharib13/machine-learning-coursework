# Data

Place the original dataset files here:

```text
train_data.csv
test_data.csv
```

The CSV files are intentionally not included in this repository.

Expected training columns:

- `gender` — target
- `age`
- `fullname`
- `username`
- `biography`
- `follower_count`
- `following_count`
- `is_business`
- `is_verified`
- `is_private`

The test file should contain the same feature columns except for `gender`.
