# Generated Outputs

This folder is for files produced by running the notebook:

- `submission.csv` — predicted cancellation labels for the provided test data;
- `model.cbm` — trained CatBoost model;
- `model_artifacts.zip` — packaged generated artifacts.

These generated files are not included here because the input datasets are not bundled with the repository.
